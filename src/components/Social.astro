---
const { platform, username } = Astro.props;
---
<a class="text-[--primary-color] text-[1rem] px-4 py-1 hover:opacity-75 focus:bg-transparent" href={`https://www.${platform}.com/${username}`} target="_blank">{platform}</a>